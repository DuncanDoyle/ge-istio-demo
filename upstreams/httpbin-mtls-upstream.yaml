apiVersion: gloo.solo.io/v1
kind: Upstream
metadata:
  name: httpbin-8000-mtls
  namespace: gloo-system
spec:
  discoveryMetadata:
    labels:
      app: httpbin
  kube:
    selector:
      app: httpbin
    serviceName: httpbin
    serviceNamespace: default
    servicePort: 8000
  